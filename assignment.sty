\usepackage{ifthen}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{amsmath}
\usepackage{color,soul}
\usepackage{graphics}
\usepackage{graphicx}
\usepackage{wrapfig}
\usepackage{hyperref}
\usepackage{textcomp} %registered brand
\usepackage [autostyle, english = american]{csquotes}
\usepackage{pdflscape} % For landscape mode in PDF\
\usepackage{array} % For flexible column widths
\usepackage{dirtytalk}
\usepackage{rotating} % to rotate tables, use \begin{sidewaystable} \begin{tabular} ... details of the table

\pagestyle{plain}
% Page layout: geometry must be loaded in the main document before \input{assignment.sty}.
\geometry{a4paper,left=1cm,right=1cm,top=2cm,bottom=1cm,includefoot}

% Abbreviation list (same entries as sections/0_nomenclature.tex), for the title-page table.
\newcommand{\reportnomenclaturetable}{%
{\footnotesize
\begin{tabular}{@{}p{2cm}p{12cm}@{}}
\multicolumn{2}{@{}l}{\textit{Abbreviations}} \\[0.35em]
AAA & Arrange--act--assert: a unit-test structure that separates setup, execution, and assertions. \\
CI/CD & Continuous integration and continuous delivery: automated build and test on each change. \\
CLI & Command-line interface: flags and subcommands for pipeline tools. \\
FN & False negative: a ground-truth object missed by the model. \\
FP & False positive: a detection not supported by ground truth. \\
GDPR & General Data Protection Regulation: European Union regulation on personal data processing. \\
GPU & Graphics processing unit: hardware used for accelerated training or inference. \\
IDE & Integrated development environment: editor with language services and debugging support. \\
IoU & Intersection over union: overlap measure for matching predictions to labels. \\
JSON & JavaScript Object Notation: structured report format emitted by several CLIs. \\
KPI & Key performance indicator: summary metrics such as detection rate or mask area fraction. \\
mAP & Mean average precision: validation metric for detection and segmentation quality. \\
MPS & Metal Performance Shaders: Apple GPU backend used by PyTorch on macOS. \\
NMS & Non-maximum suppression: post-processing that removes overlapping detections. \\
ONNX & Open Neural Network Exchange: portable model format produced by export. \\
OpenCV & Open Source Computer Vision Library: used for video frame I/O and blur in the pipeline. \\
PR & Pull request: proposed merge of a branch into \texttt{main} on GitHub. \\
PolyForm NC & PolyForm Noncommercial License 1.0.0: noncommercial open-source license for this repository. \\
UV & Astral \texttt{uv}: Python package and virtual-environment manager used per package. \\
YAML & YAML Ain't Markup Language: configuration format for \texttt{dataset.yaml}. \\
YOLO & You Only Look Once: object-detection and segmentation model family (YOLO26-seg here). \\
egui & Immediate-mode GUI library used by the Rust segmentator. \\
\end{tabular}%
}}

\newcommand{\duedate} {}
\newcommand{\setduedate}[1]{%
\renewcommand\duedate {\textbf{Date:}~ #1}}
\newcommand\isassignment {false}
\newcommand{\setassignment}{\renewcommand\isassignment {true}}
\newcommand{\ifassignment}[1]{\ifthenelse{\boolean{\isassignment}}{#1}{}}
\newcommand{\ifnotassignment}[1]{\ifthenelse{\boolean{\isassignment}}{}{#1}}

\newcommand{\copyrightpolicy}{
\begin{table}[h]
\begin{center}
\scalebox{0.8} {%
\begin{tabular}{|p{0.02cm}p{16cm}|}
\hline
&\\
\multicolumn{2}{|c|}{\Large\textbf{Final Project report}}\\
\multicolumn{2}{|c|}{\large\textbf{GDPR Anomymizer}}\\
\multicolumn{2}{|c|}{\large\textbf{A toolchain for video privacy preservation.}}\\
%\multicolumn{2}{|c|}{\large\textbf{Abstract}}\\
&\\
&
\noindent This project is a local toolchain for video privacy: it trains a YOLO26 segmentation model on labelled masks and blurs persons and vehicles in each frame.
\noindent Frame extraction, annotation, training, validation, and MP4 output run on the host machine through explicit CLI tools.
\par\medskip
\noindent\textbf{Note:} The current repository of this project can be found on:\newline \url{https://github.com/cguerreroto/gdpr_anonymizer}
\par\medskip
\reportnomenclaturetable
\par\medskip
{\footnotesize
\noindent\textbf{Disclosure on the use of GAI tools:} The author has used \href{https://www.grammarly.com/}{Grammarly} for spell and grammar checking, and sometimes vscode autocompletion for spelling and code formatting.
}
\\

\hline
\end{tabular}
}
\end{center}
\end{table}
}
\newcommand{\punkte}[1]{\hspace{1ex}\emph{\mdseries\hfill(#1~\ifcase#1{Points}\or{Points}\else{Points}\fi)}}


\newcommand\serieheader[6]{
\thispagestyle{empty}%
\begin{flushleft}
\includegraphics[width=1\textwidth]{CI_logo}
\end{flushleft}
  %\noindent%
  {\Large\ignorespaces{\textbf{#1}}\hspace{\fill}\ignorespaces{ \textbf{#2}}}\\ \\%
  {\large\ignorespaces #3 \hspace{\fill}\ignorespaces #4}\\
  \noindent%
  \bigskip
  \hrule\par\bigskip\noindent%
  \bigskip {\ignorespaces {\large{\textbf{#5}}}
  \hspace{\fill}\ignorespaces \small \ifthenelse{\boolean{\isassignment}}{\duedate}{#6}}
  \hrule\par\bigskip\noindent%  \linebreak
 }

\makeatletter
\def\enumerateMod{\ifnum \@enumdepth >3 \@toodeep\else
      \advance\@enumdepth \@ne
      \edef\@enumctr{enum\romannumeral\the\@enumdepth}\list
      {\csname label\@enumctr\endcsname}{\usecounter
        {\@enumctr}%%%? the following differs from "enumerate"
	\topsep0pt%
	\partopsep0pt%
	\itemsep0pt%
	\def\makelabel##1{\hss\llap{##1}}}\fi}
\let\endenumerateMod =\endlist
\makeatother




%\usepackage{textcomp} %ignore due to conflict with umlaut
\usepackage{xspace} % for u and o umlaut, as it breaks the paragraph when adding umlaut

% to make home directory
\newcommand{\dir}{\fontfamily{qcr}\selectfont \textasciitilde/} %example: the file{\dir datafile.jsonl})

% The font used for "coding" text style
\newcommand{\ff}{\fontfamily{qcr}\selectfont } %example: the field {\ff comment} works

% -------------------for the button code style-------------------
\usepackage{tcolorbox}       % For styling the box
\tcbuselibrary{skins}        % Required for shadows
\usepackage{xcolor}          % For colour definitions (already loaded by tcolorbox)

\newtcbox{\hll}{ % Creates a new tcbox command
  on line,                 % Inline box (no line breaks)
  colback=gray!10,         % Background colour (light grey)
  colframe=white,          % Border colour (white, invisible)
  boxrule=0pt,             % No border
  arc=3pt,                 % Rounded corners
  boxsep=0pt,              % No padding between text and box
  left=2pt, right=2pt,     % Horizontal padding
  top=1.5pt, bottom=1.5pt, % Vertical padding
  shadow={1.5pt}{-1.5pt}{0pt}{black!25}, % Shadow: {h-shift}{v-shift}{blur}{colour}
  enhanced,                % Required for shadows
  before upper={\rule[-0.1pt]{0pt}{10pt}\ttfamily} % Typewriter font + baseline fix
} %use it as \hll{texthere}
% ---------------------------------------------------------------------

% for strikeout text in a paragraph, not only single words as 
\usepackage[normalem]{ulem} %use with \sout{hello world}
\usepackage{soul} % \st{hellow world}
\usepackage{cancel} % for math
\setstcolor{red}

